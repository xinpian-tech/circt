name: Build and Test

on:
  push:
    branches: [master]
  pull_request:
    types: [assigned, opened, synchronize, reopened]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.sha }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

defaults:
  run:
    shell: bash

env:
  NIX_CONFIG: |-
    accept-flake-config = true

jobs:
  clang-format:
    name: Git clang-format
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          fetch-depth: 2
          submodules: false
      - name: Check C++ formatting
        env:
          PR_BASE: ${{ github.base_ref }}
        run: |
          if [[ -z "$PR_BASE" ]]; then
            diff_commit=HEAD^
          else
            git fetch --no-recurse-submodules origin "$PR_BASE"
            diff_commit=$(git rev-parse "origin/$PR_BASE")
          fi
          export DIFF_COMMIT="$diff_commit"

          # The single-quoted script is expanded by the inner Nix-shell bash.
          # shellcheck disable=SC2016
          nix develop -L .#lint --command bash -euo pipefail -c '
            git clang-format "$DIFF_COMMIT"
            git diff --exit-code --ignore-submodules
          '

  yapf:
    name: YAPF
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          fetch-depth: 2
          submodules: false
      - name: Check Python formatting
        env:
          PR_BASE: ${{ github.base_ref }}
        run: |
          if [[ -z "$PR_BASE" ]]; then
            diff_commit=HEAD^
          else
            git fetch --no-recurse-submodules origin "$PR_BASE"
            diff_commit=$(git rev-parse "origin/$PR_BASE")
          fi
          export DIFF_COMMIT="$diff_commit"

          # The single-quoted script is expanded by the inner Nix-shell bash.
          # shellcheck disable=SC2016
          nix develop -L .#lint --command bash -euo pipefail -c '
            mapfile -d "" files < <(
              git diff --name-only --diff-filter=d -z \
                "$DIFF_COMMIT" -- "*.py"
            )
            if (( ${#files[@]} == 0 )); then
              exit 0
            fi
            yapf --in-place "${files[@]}"
            git diff --exit-code -- "${files[@]}"
          '

  slang-tests:
    name: Slang test metadata
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          fetch-depth: 1
          submodules: false
      - name: Check valgrind exclusions
        run: |
          nix develop -L .#lint --command \
            python3 utils/find_slang_tests_without_valgrind.py test
          nix develop -L .#lint --command \
            python3 utils/find_slang_tests_without_valgrind.py \
              integration_test

  build-and-test:
    name: Run CIRCT Checks
    needs: [clang-format, yapf, slang-tests]
    runs-on: [self-hosted, BIGRAM]
    steps:
      - name: Select CIRCT check variants
        env:
          EVENT_NAME: ${{ github.event_name }}
          EVENT_REF: ${{ github.ref }}
        run: |
          variants=(full)
          if [[ "$EVENT_NAME" == push \
                && "$EVENT_REF" == refs/heads/master ]]; then
            variants+=(clang)
          fi
          printf 'CIRCT_CHECK_VARIANTS=%s\n' "${variants[*]}" \
            >> "$GITHUB_ENV"
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          # Nix requires revCount metadata for git+file flake inputs.
          fetch-depth: 0
          submodules: false
      - name: Read LLVM revision
        run: |
          llvm_revision=$(git ls-tree HEAD llvm \
            | awk '$1 == "160000" { print $3 }')
          [[ "$llvm_revision" =~ ^[0-9a-f]{40}$ ]]
          echo "LLVM_REVISION=$llvm_revision" >> "$GITHUB_ENV"
          mkdir -p .nix-results
      - name: Restore sccache objects
        run: |
          command -v rclone >/dev/null

          cache_root=$(mktemp -d "$RUNNER_TEMP/circt-sccache.XXXXXX")
          chmod 0777 "$cache_root"
          mkdir -m 0777 "$cache_root/circt-sccache"
          echo "SCCACHE_LOCAL_ROOT=$cache_root" >> "$GITHUB_ENV"

          # A failed restore only makes this run compile from a cold cache.
          if ! rclone copy sccache:circt-sccache \
              "$cache_root/circt-sccache" \
              --fast-list --size-only --use-server-modtime; then
            echo "::warning::Unable to restore sccache objects; continuing with an empty cache"
          fi
          sudo chmod -R a+rwX "$cache_root"
      - name: Build and test CIRCT
        timeout-minutes: 300
        run: |
          cache_root=${SCCACHE_LOCAL_ROOT:?The restore step must set SCCACHE_LOCAL_ROOT}
          [[ "$cache_root" == "$RUNNER_TEMP/"* ]]

          override=(
            --no-update-lock-file
            --override-input circt-src "git+file://$GITHUB_WORKSPACE"
            --override-input llvm-src \
              "github:llvm/llvm-project/$LLVM_REVISION"
          )

          run_variant() {
            local variant="$1"
            local check_drv

            check_drv=$(nix path-info --derivation \
              ".#checks.x86_64-linux.$variant" "${override[@]}") \
              || return
            # Different derivations can use different nixbld UIDs. Re-open
            # cache shards before mounting the host directory into the next
            # sandbox. Network access and upstream credentials remain outside.
            sudo chmod -R a+rwX "$cache_root" || return
            nix build -L \
              --option extra-sandbox-paths \
                "/run/circt-ci/sccache=$cache_root" \
              "$check_drv^*" \
              --out-link ".nix-results/$variant" \
              || return
            ".nix-results/$variant/bin/firtool" --version || return
          }

          status=0
          read -r -a variants <<< "$CIRCT_CHECK_VARIANTS"
          for variant in "${variants[@]}"; do
            if ! run_variant "$variant"; then
              status=1
            fi
          done
          exit "$status"
      - name: Prune and upload trusted sccache objects
        if: ${{ success() && github.ref == 'refs/heads/master' }}
        continue-on-error: true
        run: |
          if [[ -z "${SCCACHE_LOCAL_ROOT:-}" \
                || ! -d "$SCCACHE_LOCAL_ROOT/circt-sccache" ]]; then
            exit 0
          fi
          [[ "$SCCACHE_LOCAL_ROOT" == "$RUNNER_TEMP/"* ]]
          command -v rclone >/dev/null

          sudo chmod -R a+rwX "$SCCACHE_LOCAL_ROOT"
          rclone delete sccache:circt-sccache \
            --fast-list --min-age 7d --use-server-modtime
          # Restore preserved each object's server upload time locally. The
          # age filter prevents pruned objects from being uploaded again.
          rclone copy "$SCCACHE_LOCAL_ROOT/circt-sccache" \
            sccache:circt-sccache --fast-list --max-age 7d --size-only
      - name: Remove local sccache objects
        if: ${{ always() }}
        run: |
          cache_root=${SCCACHE_LOCAL_ROOT:-}
          if [[ -n "$cache_root" && -d "$cache_root" ]]; then
            [[ "$cache_root" == "$RUNNER_TEMP/"* ]]
            sudo find "$cache_root" -mindepth 1 -delete
            sudo rmdir "$cache_root"
          fi

  ci-result:
    name: Package PR and Run Checks
    needs:
      - clang-format
      - yapf
      - slang-tests
      - build-and-test
    if: ${{ always() && !cancelled() }}
    runs-on: self-hosted
    steps:
      - name: Require every CI job to pass
        env:
          BUILD_RESULT: ${{ needs.build-and-test.result }}
          CLANG_FORMAT_RESULT: ${{ needs.clang-format.result }}
          SLANG_TESTS_RESULT: ${{ needs.slang-tests.result }}
          YAPF_RESULT: ${{ needs.yapf.result }}
        run: |
          printf 'clang-format=%s\nyapf=%s\nslang-tests=%s\nbuild=%s\n' \
            "$CLANG_FORMAT_RESULT" "$YAPF_RESULT" \
            "$SLANG_TESTS_RESULT" "$BUILD_RESULT"
          [[ "$CLANG_FORMAT_RESULT" == success ]]
          [[ "$YAPF_RESULT" == success ]]
          [[ "$SLANG_TESTS_RESULT" == success ]]
          [[ "$BUILD_RESULT" == success ]]

  upload-dispatch-metadata:
    name: Upload Dispatch Metadata
    runs-on: self-hosted
    steps:
      - name: Write PR number
        env:
          HEAD_REPO: ${{ github.event.pull_request.head.repo.full_name }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          mkdir -p dispatch-metadata
          find dispatch-metadata -mindepth 1 -delete
          printf '%s\n' "$PR_NUMBER" > dispatch-metadata/pr_number
          printf 'head-repo=%s\nhead-sha=%s\n' "$HEAD_REPO" "$HEAD_SHA" \
            > dispatch-metadata/source.txt
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: ${{ always() }}
        with:
          name: dispatch-metadata
          path: dispatch-metadata/
          retention-days: 7
